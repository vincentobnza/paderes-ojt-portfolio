/** Placeholder until real document scans are added to `public/`. */
export const FALLBACK_IMAGE_SRC = "/fallback_image.png" as const;

export const FALLBACK_IMAGE_WIDTH = 1870;
export const FALLBACK_IMAGE_HEIGHT = 1247;
